# Состояние проекта

Обновлено: 2026-09-07. Реализован P2 (F08–F10) после перепроверки
[игрового аудита](audits/2026-09-06-gameplay-audit.md).
Решения и границы: [план P2](plans/2026-09-06-p2.md).
Предыдущий статус и проверки P1: [история P1](history/2026-09-06-p1-status.md).

## Ровная нагрузка по умолчанию (2026-09-07)

- Новые планы, новые фабрики без копирования текущего плана и сброс используют
  smooth-power. Сохранённые планы, профили и черновики сохраняют выбранную цель.
- В проверках фиксированных частот цель power теперь задаётся явно. Первый
  запуск проверяет новый порядок целей и статус допустимого приближения.
- Проверено: **181/181 Vitest, 36/36 Playwright и production build с TypeScript — PASS**.
  Полный UI-прогон выполнен на 127.0.0.1:3012 с БД в памяти; включены оба режима,
  профили, импорт/экспорт, миры, партии, клавиатура и мобильные экраны.
  Сохраняются прежние предупреждения сборки Zod PURE и размера chunk.
  Пользовательский Docker-контейнер не пересобирался.

## Автоподбор частот производств (2026-09-07)

- После уточнения пользователя цель «Ровная нагрузка» подбирает рабочие частоты
  для непрерывного выпуска. Согласованный порядок: выпуск → минимум физических
  машин → энергия → сырьё. Заданная частота — верхний предел, нижний — 1%.
  Это заменяет первоначальную оптимизацию простоев при фиксированных частотах.
- Одинаковые машины группы получают равномерную нагрузку. Для заказа 7,5
  усиленных железных пластин/мин два сборщика работают на 75% частоты и 100%
  времени. Закреплённые линии сохраняются; незакреплённые могут снизить частоту
  при том же оборудовании. Добывающие установки сохраняют частоты источников.
- Нелинейная мощность входит в модель консервативными кусочно-линейными
  ограничениями до выбора рецептов и зданий. Показанные MW пересчитываются
  точно и независимо проверяются с исходными лимитами. Статус approximate
  отображается как «Допустимое приближение»; глобальный нелинейный оптимум
  не заявляется. [Формулы и ограничения](power-smoothing.md).
- Проверено: **181/181 Vitest — PASS**. Включены выбор рецепта после подбора
  частоты, дробный заказ, транспорт, средний/пиковый лимит с резервом,
  все три политики нескольких продуктов, нижний предел частоты, линии,
  усилители, сравнения и API roundtrip. **Production build с TypeScript — PASS**;
  прежние предупреждения Zod PURE и размера chunk сохраняются.
- **8/8 выбранных Playwright-сценариев — PASS** на production-сборке:
  клавиатурный выбор и сохранение цели, инструкция 75% частоты / 100% времени,
  прежние фиксированные частоты, сравнения и отмена/устаревание worker.
  Проверочный API: 127.0.0.1:3012, БД в памяти. Полный набор UI-тестов,
  симуляция динамики сети и проверка в игре не проводились.
- Следующий шаг для контейнерного запуска — пересборка
  docker compose up --build -d; пользовательский контейнер не менялся.

## Сообщения аккаунта и верхняя панель (2026-09-07)

- Исправлена рамка поиска в выборе доступных ресурсов: фокус выделяет всю
  строку поиска, выступающий outline вложенного input убран. Общий search-field
  сохраняет видимый фокус через focus-within. Проверены снимки при ширине 1440
  и 390 px, поиск ресурса и выбор стрелкой/Enter с возвратом фокуса; 2/2
  существующих Playwright-сценариев выбора/мобильной клавиатуры и сборка — PASS.
- В профилях ошибки HTTP сопровождаются кодом, русским пояснением и следующим
  действием: требования к данным, неверный пароль, запрещённый адрес, занятый
  логин, недоступный маршрут, размер запроса, частые попытки и сбой сервера.
  Сетевой сбой объясняется отдельно без выдуманного HTTP-кода; HTML-ответ 5xx
  также получает понятное сообщение. Конфликт профиля не подменяется занятым логином.
- Требования к логину и паролю видны до отправки и связаны с полями через
  aria-describedby. Из верхней панели убрана надпись «Локальный черновик»;
  сохранение черновика и напоминание о сохранении открытой фабрики сохранены.
- Проверено: production build с TypeScript — PASS (прежние предупреждения Zod
  и размера chunk); 5/5 выбранных Playwright-сценариев — PASS: ошибки аккаунта,
  доступные подсказки, профиль и roundtrip черновика/JSON. Проверка шла на
  127.0.0.1:3011 с БД в памяти. Полный набор тестов не запускался.
- Изменения требуют пересборки Docker: `docker compose up --build -d`.
  Работающий пользовательский контейнер в этой правке не пересоздавался.

## Контейнерный запуск (2026-09-07)

- Добавлены Dockerfile (Node.js 24, сборка web/worker и запуск API),
  docker-compose.yml, .dockerignore и .env.example с русскими пояснениями.
- Один сервис раздаёт frontend и API; SQLite сохраняется в томе app-data.
  Сервер запускается от node; healthcheck использует /api/session.
  Внешний адрес/порт и HTTPS cookies настраиваются через .env.
- README описывает запуск, обновление, HTTPS proxy и сохранность данных.
- Проверено: `docker compose --env-file .env.example config` — PASS;
  `pnpm build` (включая TypeScript и подготовку HiGHS) — PASS, прежние
  предупреждения Zod PURE и размера chunk сохраняются.
  Команда runtime `node --import tsx apps/api/server.ts` проверена локально
  с временной БД в памяти: /, /api/session и /solver/highs.wasm — HTTP 200
  с ожидаемыми типами содержимого. Проверочный процесс остановлен.
- `docker compose --env-file .env.example build` дошёл до получения базового
  образа и остановился: Docker Engine использует недоступный прокси
  127.0.0.1:3067 (connection refused). Полная сборка образа, запуск контейнера
  и сохранение тома при пересоздании пока не проверены; следующий шаг —
  повторить их после восстановления прокси Docker. Полные Vitest/Playwright
  в этой задаче не запускались: код приложения не менялся.

## Доработка остатка аудита

План: [F03/F04/F10/F11](plans/2026-09-06-audit-followup.md).

- F03: импортированы 10 ветвей MAM (110 узлов, включая 13 сезонных) и 8 фаз
  HUB; показаны цепочки закрытых рецептов, OR родителей/видимости и AND-of-OR
  условий схем. Пять неизвестных координатных связей сохранены явно.
- F04: 28 русских игровых категорий, принадлежность 152 предметов и 276 рецептов,
  37 переопределений категорий. Ручная эвристика классификации удалена.
- F10: плавильня поддерживает один Somersloop по общим настройкам подсистемы;
  стоимость компенсатора и всех настроенных спутников включена в ведомость.
- F11: восстановление фокуса, клавиатурный выбор, ошибки числового ввода,
  краткие live-статусы, доступные диалоги, skip-link и исправление перекрытия
  заголовка закреплённым меню на мобильном экране.

Первичные источники и ограничения: [извлечение assets](audits/2026-09-06-game-assets.md).
Подготовлены
[протокол и три импортируемых плана](audits/2026-09-06-player-validation.md)
для испытаний с игроками; проведение с участниками не заявляется.

## Реализовано в P2

| Пункт | Результат |
| --- | --- |
| F08 | Существующие производственные линии с собственными рецептами, частотой, количеством, усилителями и долей работы; закрепление выпуска и использование свободной мощности; отдельные решения «оставить / добавить / перестроить»; ведомость новых зданий отдельно от существующих |
| F08, снабжение | Имена, заметки, конечный резерв и заданная либо неизвестная энергия импорта; общие конечные узлы мира и квоты фабрик; проверка суммы при сохранении workspace и внутри одного плана; копирование узлов при импорте |
| F09 | Партия: требуемое количество, запас и срок; цель в минуту из остатка; готовые позиции не требуют производства; оценка срока по выпуску после стационарного режима; отдельная достижимая доля невыполнимого заказа |
| F10 | Конечный целочисленный бюджет Somersloops, выбор распределения при заданных частотах; мощности и усиленные выходы входят в модель и независимую проверку; скважина с одним компенсатором и группами спутников; конечные отгрузки побочных продуктов и рекомендации запуска SCC-контуров |

P1 сохранён: два режима, три политики, минимумы/максимумы, средняя и пиковая
мощность с резервом, цели энергии/сырья/числа машин, сравнение рецептов,
русский поиск, построение, миры и профили. Новые поля Plan необязательны;
старые планы V1 читаются без разрушительной миграции. Sankey не добавлялся.

## Данные и расчёт

- Основной каталог steam-24656030-v1 сохранён: 152 предмета, 276 рецептов,
  110 альтернативных. Основной генерируемый каталог вручную не правился.
- p2-mechanics.json воспроизводимо импортируется из закреплённых Docs и assets
  через scripts/import-p2-mechanics.mjs; основной data:import также вызывает его.
  Сохранены SHA-256, версия каталога, дата и исходные свойства классов.
- Усиление: выход ×(1+s/slots), мощность ×(1+s/slots)². Бюджет занят целыми
  физическими машинами, в том числе простаивающими существующими линиями.
- Скважина: 60 м³/мин при обычной чистоте и 100%, мощность компенсатора
  150 МВт × clock^log2(2.5). Каждый спутник ограничивается своей трубой.
- Контроль: один усиленный конструктор в ручном примере — 40 изделий/мин,
  16 МВт, один Somersloop; два спутника при 10 м³/мин — один компенсатор 150 МВт.
- Контроль партии: 100 требуется, 40 на складе, 30 минут → 2/мин.
  Контроль квот: 90+40 из 120 отклоняется без изменения сохранённого пространства.

## Проверки

- **163/163 Vitest — PASS**, включая партии, линии/усилители/скважины/экспорт,
  квоты, переходы миров, roundtrip API, категории и граф исследований.
- **TypeScript --noEmit — PASS** (также входит в production build).
- **Production build — PASS**. Прежние предупреждения Zod PURE; основной
  JS chunk около 779 kB, 179 kB gzip, больше рекомендуемых 500 kB.
- **32/32 сценария Playwright Chrome — проверены** на production-сборке:
  полный прогон 31 PASS, последний сценарий ожидал устаревшую подпись об
  отсутствии графа MAM. После замены ожидания на новый справочник повторный
  `--last-failed` — 1/1 PASS. Включены 9 сценариев доступности и 2 исследования;
  мобильное исправление перекрытия заголовка дополнительно осмотрено по снимку.
- Независимые обзоры core и интеграции: PASS; дополнительно проверены 36 аналитических
  комбинаций бюджетов/типов зданий/целей. Импорт механик побайтово воспроизводим.
- Новые независимые обзоры стоимости скважин, доступности, категорий/слотов и
  графа исследований — PASS. Повторный офлайн-импорт побайтово воспроизвёл
  catalog, audit-report, progression и p2-mechanics.
- Найденные при интеграции ошибки исправлены и закреплены: проверки открытий
  демонтируемых линий, перенос P2 при создании мира, переход скважина→квота,
  неверные подписи энергетической границы, сумма квот внутри одного плана.

## Оставшиеся границы

- Автоподбор частот производства доступен в цели «Ровная нагрузка». Геометрия фабрики,
  длины лент, standby, фазы циклов, накопители и динамическое заполнение не моделируются.
- Семь категорий не сериализуют приоритет: их порядок и равные приоритеты
  дополняются русским алфавитом. Полное совпадение порядка меню не заявляется.
- У пяти узлов MAM есть отсутствующие координаты родителей; неизвестные связи
  не разрешают исследование. Подбор предметов, события, завершение фаз и схем
  отмечаются пользователем. Граф не является автоматическим прохождением игры.
- Скважина потребляет полную мощность компенсатора всё время использования.
  Оптимизация синхронного включения/выключения не выполняется. Спутники считаются
  установленными согласно конфигурации источника; стоимость их строительства
  и компенсатора импортирована и включается в ведомость полностью.
- «Оставить» относится к производственным линиям; добыча и Sink считаются заново.
  «Перестроить» разрешает заменить все производственные линии; возврат материалов
  при демонтаже не моделируется. Нет автоматического подбора частичного демонтажа.
- Квоты мира распределяются явно и проверяются совместно при сохранении workspace.
  Отдельные черновики не являются синхронно работающей моделью всех фабрик.
  Резерв не освобождает квоту. Внешняя энергия остаётся неизвестной, пока не задана.
- Отгрузка требует постоянного получателя, не создаёт источник другой фабрики
  автоматически и не обозначает бесконечный склад. Партии — стационарная оценка
  времени, не симуляция запуска/остановки по достижении количества.
- Ограничения численного масштаба >=0,000001/мин и общего времени анализа сохраняются.
- Полный WCAG-аудит, живой screen reader и тестирование с игроками не проводились.

## Рабочая копия и запуск

- Ветка codex/p2-gameplay. На начало задачи Git фактически находился на master,
  прежняя запись STATUS о codex/calculator-v1 была неактуальна.
- Существовавшие untracked docs/audits, docs/history и docs/plans сохранены;
  изменения не закоммичены. Пользовательская .data, зависимости, vendor, dist и
  output не включаются в Git; проверенные компактные источники игры включены.
- Chrome проверяется на 127.0.0.1:3010 с отдельной .data/p2-tests.sqlite.
  Пользовательская база не использовалась для браузерных тестов; проверочный сервер остановлен.
- Обычный запуск: pnpm dev / node scripts/dev.mjs. Готовая версия: pnpm build,
  затем pnpm start. Подробности в README.
- Следующий шаг: провести подготовленные сценарии с игроками и настоящим
  скринридером; полная аттестация WCAG и совпадение порядка меню не заявляются.
  Динамическая модель остаётся отдельной задачей.
